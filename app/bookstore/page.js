export const metadata = {
  title:
    "ITAN African Bookstore | Explore a Wide Category of African Stories",
};

import BookStoreClient from "@/SEOComponet/bookstore";

export default function BookStore() {
  return <BookStoreClient />;
}
